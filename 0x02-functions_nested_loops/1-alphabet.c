#include <stdio.h>
#include "main.h"

/**
 * print_alphabet - display the alphabet in lowercase
 * @a: our parameter
 * Return: 0
 */
int print_alphabet(int a)
{
	for (int a == 'a'; a <= 'z'; a++)
	{
		_putchar(a);
	}
	_putchar('\n');
	return (0);
}
