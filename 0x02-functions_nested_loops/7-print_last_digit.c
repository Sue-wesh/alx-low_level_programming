#include <stdio.h>
#include "main.h"

/**
 * print_last_digit - find the last digit
 *
 * @n: our parameter
 *
 * Return: 0 at the end
 */
int print_last_digit(int n)
{
	int a;

	if (n == 98)
	{
		a = n % 10;
		_putchar(a);
		else if (n == 0)
		{
			a = n % 10;
			_putchar(a);
			else if (n == -1024)
			{
				a = n % 10;
				_putchar(a);
				else if (n == 4)
				{
					a = n % 10;
					_putchar(a);
				}
			}
		}
	}
	return (0);
}
