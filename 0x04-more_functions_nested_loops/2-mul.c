#include "main.h"
#include <stdio.h>

/**
 * mul - multiply two integers
 * @a: parameter first
 * @b: second parameter
 * Return: Always 0
 */
int mul(int a, int b)
{
	int x;

	x = a * b;

	return (x);
}
