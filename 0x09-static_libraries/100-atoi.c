#include <stdio.h>
#include "main.h"
#include <stdlib.h>

/**
 * _atoi - convert string to integer
 * @s: the pointer to the string
 * Return: the value to be displayed
 */
int _atoi(char *s)
{
	int x = atoi(s);

	return (x);
}

/**
 * main - check code
 * Return:0
 */
int main(void)
{}
