#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 * _strcmp - function to compare strings
 * @s1: the first string
 * @s2: the second string
 * Return: the result
 */
int _strcmp(char *s1, char *s2)
{
	int x;

	x = strcmp(s1, s2);
	return (x);
}

/**
 * main - code
 * Return:0
 */
int main(void)
{}
